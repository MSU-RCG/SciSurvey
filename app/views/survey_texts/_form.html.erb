<%= form_for(@survey_text) do |f| %>
  <% if @survey_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_text.errors.count, "error") %> prohibited this survey_text from being saved:</h2>

      <ul>
      <% @survey_text.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
